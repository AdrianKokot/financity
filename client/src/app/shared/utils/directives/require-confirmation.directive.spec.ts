import { RequireConfirmationDirective } from './require-confirmation.directive';

describe('RequireConfirmationDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireConfirmationDirective();
    expect(directive).toBeTruthy();
  });
});
